project:
  type: default
  output-dir: ../docs

execute:
  freeze: auto  # Re-render only when source changes
  seed: 123      # Reproducible random results for all documents
  warning: false
  echo: true

format:
  gfm:
    variant: +yaml_metadata_block
