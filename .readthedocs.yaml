# .readthedocs.yaml
version: 2

mkdocs:
  configuration: mkdocs.yml

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"
  jobs:
    post_install:
      - pip install pdm
      - pdm install --prod
      - pdm install -G docs --no-self
